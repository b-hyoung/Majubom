# macOS
.DS_Store

# Python venv / cache
esp32/venv/
__pycache__/
*.pyc
*.pyo

# ESP-IDF build artifacts
build/
sdkconfig.old
managed_components/
dependencies.lock

# Local IDE
.vscode/
.idea/

# 서버 런타임 상태 (침대별 baseline 누적 — 배포 환경마다 다름, 커밋 X)
server/csi_baseline.json

# Generated CSI plots (regenerable from csi_log.csv)
esp32/csi_heatmap.png
esp32/csi_timeseries.png
esp32/vital_csi.png

# CSI 측정 raw CSV — git 용량 비대 방지(향후 측정분은 커밋 안 함).
# 이미 커밋된 검증용 케이스는 예외로 유지.
esp32/*.csv
!esp32/csi_hold4.csv
!esp32/csi_rest.csv
!esp32/csi_watch1.csv

.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

작업일지.md

*.txt
*.png
# 런타임 생성 — 빈 침대 캘리브레이션 베이스라인(환경별로 다름)
server/tof_baseline.json