# 0707 ToF 진행상황 — 인수인계 (라즈베리파이 Claude용)

> 이 세션(노트북)에서 작업한 내용 정리. 라즈베리파이에서 이어서 작업할 때 참고.
> `git pull`로 코드는 받아지지만, 이 문서는 커밋 안 될 수 있으니 내용 자체를 참고해서 진행할 것.

## 오늘 한 일 요약

### 1. 8×8 업그레이드 — 실측으로 검증 완료
- 예전(`TOF/TROUBLESHOOTING.md` #3)에 8×8에서 I2C 버스 wedge 나서 4×4로 내렸던 이력 있음.
- 지금은 **센서마다 독립 ESP32 + 독립 I2C 버스** 구조(예전엔 버스 하나에 센서 둘 공유)라 원인이 해소됐는지 실측함.
- `test_8x8/` 폴더(프로덕션과 분리된 테스트용)로 tof1·tof2 각각 8×8 켜서 **수 분간 연속 관찰 → 버스 wedge 재현 안 됨** 확인.
- 이후 `TOF/src/main.ino`를 실제로 8×8로 전환(`ZONE_COUNT=64`, `GRID_SIDE=8`, `resolution="8x8"`), 전송 스로틀도 100ms→10ms로 낮춰서 센서 자체 상한(15Hz)까지 나오게 함.
- `TOF/src/platformio.ini`: `lib_extra_dirs`(로컬 경로, 이식 안 됨) → `lib_deps`(PlatformIO 레지스트리, 어디서든 빌드됨)로 변경.
  - `stm32duino/STM32duino VL53L5CX@^1.2.3`, `bblanchon/ArduinoJson@^7`

### 2. 서버 쪽 변경 (8×8 대응)
- `server/db.py`: `TOF_ZONES` 16→64 (컬럼 d0..d63/t0..t63로 자동 재생성됨, DB는 아직 없어서 마이그레이션 불필요했음)
- `server/tof_server.py`:
  - `PRESENCE_MIN_ZONES` 2→8, `STAY_MIN_ZONES` 1→4 (존 개수 4배 늘어난 것에 비례 조정 — **실측 미검증치, 나중에 확인 필요**)
  - `app.run(..., threaded=True)` 추가 — 싱글스레드라 tof1·tof2 동시 POST 시 서로 막던 문제 해결
  - `/`(루트) 페이지를 1초 meta-refresh → **JS 폴링(80ms) 실시간 그리드**로 전면 교체 (tof1/tof2 색상 그리드 + 재실뱃지 + 자세 표시, 새로고침 없이 실시간 갱신)

### 3. ROI/기하 버그 두 개 발견·수정 (`TOF/ml/roi.py`)
- **좌우반전**: 실측(손으로 짚어보며 확인)해보니 tof1·tof2 둘 다 좌우가 반대로 나옴 → `hit_y`/`hit_point`의 열(column) 각도 계산 부호 반전
- **그리드 중심좌표 버그**: GRID을 4→8로 바꾸면서 중심 오프셋이 옛날 값(1.5, 4×4 기준)으로 하드코딩된 채 남아있었음 → `(GRID-1)/2`(=3.5)로 동적 계산하도록 수정
  - 이 버그가 "그리드 절반만 값 보이는" 현상의 실제 원인 중 하나였을 가능성 높음
- `TOF/tof_3d_posture.html` + `server/tof_3d.html` (뷰어, 항상 동기화 유지): 같은 좌우반전 버그 수정(`flipLR` 기본값 true), 같은 중심좌표 버그 수정. **덤으로 발견**: `server/tof_3d.html`엔 이전 커밋(머리/발 스왑)이 반영돼 있었는데 `TOF/tof_3d_posture.html`엔 동기화가 안 돼 있었음 → 지금 맞춤.
- **발쪽 센서 실측 기울기 80도로 변경**(사용자가 물리적으로 조정) → `roi.py`의 `TILT1_DEG`(발=tof1 쪽 상수, 90→80), 뷰어의 `TILT2`(발 슬롯, 65→80) 둘 다 반영.
  - ⚠️ 주의: `roi.py`는 `tof1`이 `TILT1_DEG`와 묶여있고, 뷰어(스왑 수정 이후)는 "발 슬롯"이 `TILT2` 변수 — **같은 물리량인데 두 파일에서 변수명이 다름**. 헷갈리지 말 것.

### 4. 물리적 사실 (헷갈리기 쉬움, 꼭 기억)
- **tof1 = 발(foot) 센서, tof2 = 머리(head) 센서** — 펌웨어 SENSOR_NAME과 실제 위치가 이렇게 배정됨(원래 반대로 예상했던 듯). 3D 뷰어는 이미 이걸 보정해서 화면엔 올바르게 표시됨(`server/tof_3d.html`의 `sensors()` 함수 참고).

### 5. 자세분류 모델 — 삭제함, 재학습 필요
- 오늘 8×8로 재수집 + 재학습해서 6클래스(`empty/supine/side_left/side_right/sitting/edge_sit`) 모델을 만들었었는데(`14909c5` 커밋), **위 ROI 버그(좌우반전 미적용 + 그리드중심 오류) 상태로 학습된 것**이라 신뢰 불가 판단 → `TOF/ml/posture_model.joblib` **삭제함**.
- 원본 raw 데이터(`TOF/dataset/tof_*_8x8_*`)는 안 지웠음 — raw distances_mm 자체는 안 틀렸고 ROI 처리 단계 버그였어서, 새로 찍는 데이터에 **그대로 추가**하기로 함.

### 6. 데이터 재수집 진행 중 (균형 400프레임/클래스 기준)
목표:
| 클래스 | 목표 | 상태 |
|---|---|---|
| empty | 400 | ✅ 완료 (`tof_empty_20260707_161246.jsonl`) |
| supine | 400 | ⏸️ 중단됨(일부만 캡처됨, `tof_supine_20260707_162613.jsonl` — 프레임 수 적음, 이어서 하거나 다시 찍어야 함) |
| side_left | 400 | 미시작 |
| side_right | 400 | 미시작 |
| sitting | 400 | 미시작 |
| edge_sit(중앙) | ~134 | 미시작 |
| edge_sit(머리쪽) | ~133 | 미시작 |
| edge_sit(발쪽) | ~133 | 미시작 |

캡처 명령: `cd TOF && python capture_dataset.py <라벨> <프레임수>` (환경변수 `TOF_SERVER`로 서버 주소 override 가능, 기본은 localhost — 파이에서 돌릴 땐 그대로 두면 됨. `TOF/capture_dataset.py` 안에 `RPI = os.environ.get("TOF_SERVER", "http://localhost:5001/tof/latest")`로 이미 바뀌어 있음)

**나중 계획**: 이후 "다른 사람"으로 전체 클래스 한 번 더 수집 예정(일반화 검증용, leave-one-person-out 평가하려고 파일명에 사람 구분 태그 추천됨 — 예: `tof_supine_personB_...`).

전부 다 모이면 `cd TOF/ml && python train_posture.py`로 재학습.

### 7. 서버 실행 위치 — 노트북→파이로 다시 전환 중
- 노트북 사양 문제로 잠깐 이 PC(192.168.6.20)에서 `tof_server.py` 돌리며 작업함 (Python/PlatformIO/scikit-learn/joblib/numpy/torch 등 이 PC에 새로 설치함, `requirements.txt`엔 flask 계열만 있고 ML 의존성은 없어서 별도 설치했음).
- 지금 다시 **라즈베리파이에서 서버 실행**으로 전환하는 중. `TOF/src/main.ino`의 `SERVER_URL`을 파이 IP(`192.168.6.10:5001`)로 되돌리는 작업 진행.
- 파이에서: `git pull` 후 `cd server && python run_all.py` (CSI는 완전 제거 예정이라 CSI 관련 에러/경고 있어도 무시 가능 — 나중에 `run_all.py`에서 CSI 빼는 작업 필요할 수 있음)
- torch(이상탐지)는 이 PC에선 VC++ 재배포 패키지 설치 후 정상 작동 확인함 — 파이(Linux)는 원래 이런 DLL 이슈 없을 것.

### 8. CSI — 완전 제거 결정됨
- 사용자가 "CSI는 없어도 된다, 완전히 없애기로 했다"고 확정함. 아직 코드에서 실제로 안 지웠음 — `csi_server.py`, `run_all.py`의 CSI 관련부, `site/index.html`의 CSI 패널 등 정리는 별도 작업으로 남아있음.

## 다음에 할 일 (우선순위)
1. 파이에서 서버 재시작 후 보드 연결 확인
2. supine 이어서/다시 찍기 → side_left → side_right → sitting → edge_sit×3
3. 전부 모이면 `train_posture.py` 재학습, 시간분할(time split, 기본값)로 실제 정확도 확인 — **random split은 낙관적이라 지양**
4. YAW_DEG/Y_OFF (좌우 정렬 캘리브레이션)는 아직 0,0 — `TOF/ml/calibrate_roi.py`로 실측 필요 (미완료 항목)
5. CSI 제거 작업 (csi_server.py, run_all.py, site/index.html)
