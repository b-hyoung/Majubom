# 0704 할 일 (ToF 자세/재실)

> 어제(0703) 정리: 자세 데이터 수집(5클래스 19,000프레임) → RandomForest 학습(정확도 91.9%)
> → 서버·대시보드 통합 → 침대폭 ROI 적용까지 완료. **ROI 좌우 정렬 보정 중간에 멈춤.**
> origin/main 최신 커밋: `558c605`

---

## ⭐ 최우선 — ROI 좌우 정렬 보정 (어제 하다 멈춤)
**문제:** tof1을 12시(머리)에 놓으면 왼쪽은 침대끝 잘 맞는데 **오른쪽이 20cm 정도 침대 밖까지 인식**.
원인 = 센서가 정중앙·정면이 아니라 살짝 회전/치우침. (ROI는 정중앙 가정)

**할 일:**
1. 침대 **오른쪽 가장자리에 기준**(눕거나 상자/베개) 두고 가만히
2. 로컬에서 측정:
   ```bash
   cd TOF/ml
   python calibrate_roi.py --server http://192.168.6.10:5001
   #  오른쪽 가장자리 존들의 Y(m)가 실제 침대끝(+0.475)에 오도록
   #  --yaw1 값(부호 바꿔가며) 조정. 예: --yaw1 -6
   ```
3. 맞는 값 찾으면 `TOF/ml/roi.py` 상단 `YAW_DEG["tof1"]`(필요시 `Y_OFF`)에 반영
4. 재학습(ROI가 바뀌었으니):
   ```bash
   python train_posture.py       # posture_model.joblib 갱신
   ```
5. 커밋 (roi.py + posture_model.joblib). predict/서버는 자동 반영됨.

---

## 라즈베리파이 반영 & 실전 확인
어제 올린 서버/모델 변경들이 파이에 아직 안 들어갔을 수 있음.
```bash
cd ~/Majubom && git pull origin main
pip install scikit-learn joblib numpy      # 자세 모델용 (1회)
cd server && python run_all.py             # 재시작
```
- 서버 로그에 `[posture] 자세 분류 모델 로드됨` 확인
- 대시보드(`http://192.168.6.10:5003/dashboard`) ToF 타일에 **실시간 자세** 뜨는지 확인

---

## 누움→빈침대 오인 (실전에서 제일 잦음)
ROI 반영 후 실제로 나아졌는지 대시보드에서 확인.
**아직도 누움을 빈침대로 오인하면** (빈침대 데이터 늘리는 건 역효과! ↓ 순서로):
1. 위 ROI 보정이 제대로 됐는지 재확인 (침대밖 잡음이 원인일 수 있음)
2. **누운 자세(occupied)를 다양하게 추가 수집** (빈침대 말고 이쪽)
3. **특징 보강** — "빈 침대 기준 대비 가까운 존 수"를 feature에 추가 (재실 신호 강화)
4. **역할 분리** — 재실/이탈=서버 베이스라인 로직, ML=자세만

---

## supine↔side_right 혼동 (테스트 약점)
- 정확도 91.9%인데 supine recall 0.68 (일부를 side_right로 오인)
- 개선: **supine을 다른 위치·팔다리 변형으로 추가 수집** 후 재학습
  ```bash
  cd TOF && python capture_dataset.py supine 3000   # 다른 위치로!
  python preprocess_dataset.py                       # 필터본
  cd ml && python train_posture.py
  ```

---

## 데이터 2단계 (일반화)
- **다른 사람 체격**으로 각 클래스 추가 (지금은 1명·1세션이라 낙관적)
- 침대 위 **다른 위치**(위/아래/좌우 치우침), **이불 유무** 바꿔서
- 양보다 **다양성**이 중요

---

## 참고 (파일/역할)
| 파일 | 역할 |
|------|------|
| `TOF/capture_dataset.py` | 데이터 수집 (`python capture_dataset.py <label> <count>`) |
| `TOF/preprocess_dataset.py` | 스파이크 제거(median w5) 필터본 생성 |
| `TOF/ml/train_posture.py` | 학습·평가 → `posture_model.joblib` |
| `TOF/ml/predict_posture.py` | 실시간 자세 예측 (서버가 import) |
| `TOF/ml/roi.py` | 침대 폭 ROI + 좌우정렬 보정(YAW/OFFSET) |
| `TOF/ml/calibrate_roi.py` | ROI 좌우정렬 캘리브레이션 도구 |
| `TOF/dataset/` | 수집 데이터(+README) |
| `TOF/ml/README.md` | 모델 실행 가이드 |

## 커밋 규칙
- 로컬 뷰어 수정 시 → `server/tof_3d.html`에도 복사(동기화) 후 커밋
- 서버/모델 변경 → 파이에서 `git pull` + `run_all.py` 재시작해야 반영
